<% content_for :title, "New content" %>

<h1 class="font-bold text-4xl mbe-4">New content</h1>

<%= render "form", content: @content %>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to "Back to contents", contents_path, class: "btn" %>
</div>
